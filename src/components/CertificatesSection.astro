---
import CardCertificate from "./CardCertificate.astro";

import { getCertificatesCardWithTech } from "src/utils/certificates";

const certificates = getCertificatesCardWithTech();

const limit = 6;
---

<div class="grid gap-4 py-4 grid-cols-1 md:grid-cols-2 lg:grid-cols-3">
  {
    certificates
      .slice(0, limit)
      .map((certificate) => <CardCertificate {certificate} />)
  }
</div>
